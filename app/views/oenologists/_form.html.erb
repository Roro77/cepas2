<%= form_with(model: oenologist) do |form| %>
  <% if oenologist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(oenologist.errors.count, "error") %> prohibited this oenologist from being saved:</h2>

      <ul>
        <% oenologist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="field">
    <%= form.label :nationality %>
    <%= form.text_field :nationality %>
  </div>
  
  <%= form.fields_for :oenologist_magazines do |f| %>

  <div class = "field">
    <%= f.label :magazine_id %>
    <%= f.select :magazine_id, @magazines, prompt: "Selecciona una alternativa"%>
  </div>

  <div class = "field">
    <%= f.label :job %>
    <%= f.text_field :job %>
  </div>

    <% end %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
